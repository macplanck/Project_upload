######################################
###        ARG PARAMETER           ###
######################################
in_LNUM=8
in_NAME='MatMul'

if [ -n "$1" ]; then
    in_NAME="${1}"
fi

######################################
###          Make Command          ###
######################################
make LUT_init LUT=LUT_gen LNUM=$in_LNUM NAME=$in_NAME
